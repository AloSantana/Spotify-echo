# MCP Server Environment Configuration
# Copy this to .env.mcp and configure your tokens

# =====================================
# CORE MCP SERVERS - CODING AGENT INTEGRATION
# =====================================

# Sequential Thinking MCP Server (REQUIRED)
# Enables multi-step reasoning and planning for coding agent
# No configuration needed - automatically enabled

# MongoDB MCP Server (REQUIRED)
# Database operations and user settings management
MONGODB_URI=mongodb://localhost:27017
MONGODB_DATABASE=spotify-echo
MONGODB_CONNECTION_TIMEOUT=5000

# Spotify API MCP Server (REQUIRED)
# Native Spotify integration for music data and recommendations
# Create app at: https://developer.spotify.com/dashboard/applications
SPOTIFY_CLIENT_ID=your_spotify_client_id_here
SPOTIFY_CLIENT_SECRET=your_spotify_client_secret_here
SPOTIFY_REDIRECT_URI=http://localhost:8888/callback

# SQLite MCP Server (REQUIRED)
# Local database operations for caching and offline support
SQLITE_DB_PATH=./data/spotify.db

# Playwright MCP Server (REQUIRED)
# Browser automation for testing and web scraping
# No additional configuration needed

# Hyperbrowser MCP Server (REQUIRED)
# Advanced browser automation with AI-powered capabilities
# Sign up at: https://hyperbrowser.ai to get your API key
HYPERBROWSER_API_KEY=your_hyperbrowser_api_key_here

# =====================================
# TIER 1 - ESSENTIAL SERVERS
# =====================================

# GitHub MCP Server (REQUIRED)
# Create at: https://github.com/settings/tokens
# Permissions needed: repo, issues, pull_requests, actions
GITHUB_TOKEN=ghp_your_github_token_here

# =====================================
# n8n WORKFLOW AUTOMATION (TIER 2)
# =====================================

# n8n API Configuration
# Self-hosted n8n instance configuration
N8N_API_URL=https://your-n8n-instance.com
N8N_API_KEY=your_n8n_api_key_here
N8N_API_URL_FALLBACK=http://localhost:5678
N8N_USERNAME=your_n8n_username
N8N_PASSWORD=your_n8n_password

# n8n MCP Configuration
N8N_MCP_PORT=3019
N8N_MCP_MODE=stdio
N8N_MCP_LOG_LEVEL=info

# =====================================
# TIER 3 - SPECIALIZED SERVERS (OPTIONAL)
# =====================================

# Brave Search API (OPTIONAL)
# Free tier: 2000 queries/month
# Sign up at: https://api.search.brave.com/app/keys
BRAVE_API_KEY=your_brave_api_key_here

# Perplexity AI API (OPTIONAL - ENHANCED RESEARCH)
# Advanced research with grok-4, sonar-pro, GPT-5 models
# Sign up at: https://www.perplexity.ai/settings/api
PERPLEXITY_API_KEY=pplx_your_perplexity_api_key_here

# Perplexity Configuration
PERPLEXITY_MODEL=sonar-pro
PERPLEXITY_MAX_LATENCY_MS=3000
PERPLEXITY_COST_BUDGET_USD=0.50
PERPLEXITY_BASE_URL=https://api.perplexity.ai

# =====================================
# ADVANCED MCP SERVERS (OPTIONAL - TIER 4)
# =====================================

# Docker MCP Server (OPTIONAL)
# Container management and orchestration
DOCKER_HOST=unix:///var/run/docker.sock

# Time/Calendar MCP Server (OPTIONAL)
# Scheduling and time-based operations
# No additional configuration needed

# Google Drive/Sheets MCP (OPTIONAL)
# Data export and cloud storage integration
# Create OAuth credentials at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your_google_client_id_here
GOOGLE_CLIENT_SECRET=your_google_client_secret_here
GOOGLE_REDIRECT_URI=http://localhost:8888/callback

# =====================================
# WORKSPACE CONFIGURATION
# =====================================

# Current workspace folder (auto-detected in most cases)
WORKSPACE_FOLDER=.

# =====================================
# MCP SERVER PORTS (OPTIONAL)
# =====================================

# Ports for local MCP server instances (if running locally)
MCP_GITHUB_PORT=3001
MCP_FILESYSTEM_PORT=3002
MCP_MEMORY_PORT=3003
MCP_MONGODB_PORT=3004
MCP_SPOTIFY_PORT=3005
MCP_SQLITE_PORT=3006
MCP_PLAYWRIGHT_PORT=3007
MCP_HYPERBROWSER_PORT=3008

# =====================================
# SECURITY SETTINGS
# =====================================

# Enable/disable specific server tiers
MCP_ENABLE_TIER1=true
MCP_ENABLE_TIER2=true
MCP_ENABLE_TIER3=false
MCP_ENABLE_TIER4=false

# Enable debug logging
MCP_DEBUG=false

# Enable MCP server health checks
MCP_HEALTH_CHECK_ENABLED=true
MCP_HEALTH_CHECK_INTERVAL=60000

# Enable automatic reconnection
MCP_AUTO_RECONNECT=true
MCP_RECONNECT_INTERVAL=5000
MCP_MAX_RECONNECT_ATTEMPTS=5

# =====================================
# INSTRUCTIONS
# =====================================
# 1. Copy this file to .env.mcp
# 2. Replace placeholder tokens with real values
# 3. Only configure the servers you plan to use
# 4. Keep sensitive tokens secure and never commit them
# 5. Use environment-specific files (.env.mcp.local, .env.mcp.production)
# 6. Run 'npm run mcp:start' to launch all configured MCP servers
# 7. Run 'npm run mcp:health' to check server status
# 8. Run 'node scripts/start-coding-agent-with-mcp.js' for enhanced coding agent
