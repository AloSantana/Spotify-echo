name: AWS Bedrock Live Validation

on:
  workflow_dispatch:
    inputs:
      skip_validation:
        description: 'Skip validation (for testing workflow only)'
        required: false
        default: 'false'
  push:
    branches:
      - 'copilot/fix-*'
  pull_request:
    branches:
      - main
    paths:
      - 'src/infra/BedrockInferenceProvider.js'
      - 'scripts/validate-bedrock-live.js'

jobs:
  validate-bedrock:
    name: Validate AWS Bedrock Integration
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
      
      - name: Install dependencies
        run: npm ci
      
      - name: Run AWS Bedrock Live Validation
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION || 'us-east-1' }}
        run: |
          echo "🚀 Starting AWS Bedrock Live Validation"
          echo "================================================"
          echo "⚠️  This will make actual AWS API calls and incur charges!"
          echo "================================================"
          echo ""
          echo "Checking for AWS credentials..."
          if [ -z "$AWS_ACCESS_KEY_ID" ]; then
            echo "❌ AWS_ACCESS_KEY_ID is not set!"
            echo "Please ensure secrets are configured in repository settings"
            exit 1
          else
            echo "✅ AWS_ACCESS_KEY_ID: ${AWS_ACCESS_KEY_ID:0:4}...${AWS_ACCESS_KEY_ID: -4}"
          fi
          
          if [ -z "$AWS_SECRET_ACCESS_KEY" ]; then
            echo "❌ AWS_SECRET_ACCESS_KEY is not set!"
            exit 1
          else
            echo "✅ AWS_SECRET_ACCESS_KEY: ***${AWS_SECRET_ACCESS_KEY: -4}"
          fi
          
          echo "✅ AWS_REGION: $AWS_REGION"
          echo ""
          
          if [ "${{ github.event.inputs.skip_validation }}" = "true" ]; then
            echo "⏭️  Skipping validation (skip_validation=true)"
            exit 0
          fi
          
          echo "Running validation script..."
          node scripts/validate-bedrock-live.js || {
            echo "❌ Validation script failed with exit code $?"
            echo "Check the logs above for error details"
            exit 1
          }
          
          echo ""
          echo "✅ Validation completed successfully!"
      
      - name: Upload validation report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: bedrock-validation-report
          path: reports/bedrock-validation-report.json
          retention-days: 30
      
      - name: Comment on PR with results
        if: github.event_name == 'push' && always()
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const path = require('path');
            
            try {
              const reportPath = path.join(process.env.GITHUB_WORKSPACE, 'reports', 'bedrock-validation-report.json');
              
              if (!fs.existsSync(reportPath)) {
                console.log('No validation report found');
                return;
              }
              
              const report = JSON.parse(fs.readFileSync(reportPath, 'utf8'));
              
              let comment = '## 🚀 AWS Bedrock Live Validation Results\n\n';
              comment += `**Timestamp:** ${report.timestamp}\n`;
              comment += `**Region:** ${report.region}\n`;
              comment += `**Credentials Found:** ${report.credentialsFound ? '✅' : '❌'}\n\n`;
              
              comment += '### 🤖 Models Tested\n\n';
              report.models.forEach(model => {
                if (model.success) {
                  comment += `✅ **${model.displayName}**\n`;
                  comment += `- Model ID: \`${model.modelId}\`\n`;
                  comment += `- Latency: ${model.latency}ms\n`;
                  comment += `- Tokens: ${model.usage.input_tokens} input + ${model.usage.output_tokens} output\n`;
                  comment += `- Cost: $${model.cost.toFixed(6)} USD\n`;
                  comment += `- Response: "${model.response.substring(0, 100)}..."\n\n`;
                } else {
                  comment += `❌ **${model.displayName}**: ${model.error}\n\n`;
                }
              });
              
              comment += `### 💰 Total Cost: $${report.totalCost.toFixed(6)} USD\n\n`;
              
              if (report.errors.length > 0) {
                comment += '### ⚠️ Errors\n\n';
                report.errors.forEach(error => {
                  comment += `- ${error}\n`;
                });
                comment += '\n';
              }
              
              comment += '### 📊 Next Steps\n\n';
              comment += '1. Verify charges appear in AWS Cost Explorer\n';
              comment += '2. Check CloudWatch Logs for Bedrock invocations\n';
              comment += '3. Confirm model IDs and usage metrics\n';
              
              // Find PR number from commit
              const { data: prs } = await github.rest.repos.listPullRequestsAssociatedWithCommit({
                owner: context.repo.owner,
                repo: context.repo.repo,
                commit_sha: context.sha
              });
              
              if (prs.length > 0) {
                await github.rest.issues.createComment({
                  owner: context.repo.owner,
                  repo: context.repo.repo,
                  issue_number: prs[0].number,
                  body: comment
                });
              }
            } catch (error) {
              console.error('Error posting comment:', error);
            }
