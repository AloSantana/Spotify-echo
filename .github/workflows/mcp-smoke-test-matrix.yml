name: MCP Smoke Test with Matrix Variants

on:
  push:
    paths:
      - 'scripts/**'
      - 'start-mcp-servers.sh'
      - '.copilot/**'
      - 'docs/MCP_SERVER_INVENTORY.md'
      - 'docs/AGENT_OPERATIONS.md'
      - 'mcp-servers/**'
      - '.github/workflows/mcp-smoke-test-matrix.yml'
  pull_request:
    paths:
      - 'scripts/**'
      - 'start-mcp-servers.sh'
      - '.copilot/**'
      - 'docs/MCP_SERVER_INVENTORY.md'
      - 'docs/AGENT_OPERATIONS.md'
      - 'mcp-servers/**'
      - '.github/workflows/mcp-smoke-test-matrix.yml'

jobs:
  mcp-smoke-test:
    runs-on: ubuntu-latest
    
    strategy:
      matrix:
        include:
          - variant: "baseline"
            enable_community: "0"
            strict_required: "false"
            description: "Core servers only (filesystem, memory, sequential-thinking, git)"
          - variant: "community"
            enable_community: "1"
            strict_required: "false"
            description: "All servers including community (brave-search, browserbase, perplexity, etc.)"
          - variant: "strict-community"
            enable_community: "1"
            strict_required: "true"
            description: "All servers with strict validation (fails if any required server fails)"
    
    name: "MCP Smoke Test (${{ matrix.variant }})"
    
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'
          
      - name: Install dependencies
        run: |
          npm ci
          # Install MCP SDK if needed
          npm install @modelcontextprotocol/sdk
          
      - name: Setup environment for MCP testing
        run: |
          # Create necessary directories
          mkdir -p reports automation-artifacts/screenshots
          
          # Make scripts executable
          chmod +x scripts/mcp-smoke-test.sh
          chmod +x start-mcp-servers.sh
          
          # Set up minimal environment for testing (no real API keys needed for smoke test)
          echo "SKIP_ENV_LOADING=1" >> $GITHUB_ENV
          echo "NODE_ENV=test" >> $GITHUB_ENV
          
      - name: Run MCP Smoke Test - ${{ matrix.description }}
        env:
          ENABLE_COMMUNITY_MCP: ${{ matrix.enable_community }}
          MCP_STRICT_REQUIRED: ${{ matrix.strict_required }}
          SKIP_ENV_LOADING: "1"
        run: |
          echo "🧪 Running MCP Smoke Test variant: ${{ matrix.variant }}"
          echo "📋 Configuration:"
          echo "  - ENABLE_COMMUNITY_MCP: ${{ matrix.enable_community }}"
          echo "  - MCP_STRICT_REQUIRED: ${{ matrix.strict_required }}"
          echo "  - Description: ${{ matrix.description }}"
          echo ""
          
          # Run the smoke test
          bash scripts/mcp-smoke-test.sh
          
      - name: Validate test results
        run: |
          echo "🔍 Validating smoke test results..."
          
          # Check that required files were created
          if [ ! -f "reports/mcp-capabilities.json" ]; then
            echo "❌ Missing capabilities report"
            exit 1
          fi
          
          if [ ! -f "reports/mcp-start-summary.json" ]; then
            echo "❌ Missing start summary report"
            exit 1
          fi
          
          # Parse and validate JSON files
          if ! jq empty reports/mcp-capabilities.json; then
            echo "❌ Invalid capabilities JSON"
            exit 1
          fi
          
          if ! jq empty reports/mcp-start-summary.json; then
            echo "❌ Invalid start summary JSON"
            exit 1
          fi
          
          # Run validation script and capture output
          echo "📊 Running validation analysis..."
          VALIDATION_OUTPUT=$(node scripts/validate-mcp-start.js 2>/dev/null || echo '{}')
          echo "Validation output: $VALIDATION_OUTPUT"
          
          # Parse results
          REQUIRED_STARTED=$(echo "$VALIDATION_OUTPUT" | jq -r '.requiredStarted // 0')
          REQUIRED_TOTAL=$(echo "$VALIDATION_OUTPUT" | jq -r '.requiredTotal // 0')
          COMMUNITY_INCLUDED=$(echo "$VALIDATION_OUTPUT" | jq -r '.communityIncluded // false')
          
          echo "✅ Test results summary:"
          echo "  - Required servers started: $REQUIRED_STARTED/$REQUIRED_TOTAL"
          echo "  - Community servers included: $COMMUNITY_INCLUDED"
          
          # Validate expectations based on variant
          case "${{ matrix.variant }}" in
            "baseline")
              if [ "$COMMUNITY_INCLUDED" = "true" ]; then
                echo "❌ Baseline variant should not include community servers"
                exit 1
              fi
              echo "✅ Baseline validation passed"
              ;;
            "community"|"strict-community")
              if [ "$COMMUNITY_INCLUDED" = "false" ]; then
                echo "⚠️  Warning: Community variant did not include community servers (may be expected if no community servers are configured)"
              fi
              echo "✅ Community variant validation passed"
              ;;
          esac
          
      - name: Upload test artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: mcp-smoke-test-${{ matrix.variant }}-${{ github.run_number }}
          path: |
            reports/
            automation-artifacts/
            mcp-startup.log
            mcp-*.json
          retention-days: 7
          
      - name: Create test summary
        if: always()
        run: |
          echo "## 🧪 MCP Smoke Test Results - ${{ matrix.variant }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Configuration:**" >> $GITHUB_STEP_SUMMARY
          echo "- Enable Community MCP: \`${{ matrix.enable_community }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Strict Required: \`${{ matrix.strict_required }}\`" >> $GITHUB_STEP_SUMMARY
          echo "- Description: ${{ matrix.description }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ -f "reports/mcp-start-summary.json" ]; then
            echo "**Start Summary:**" >> $GITHUB_STEP_SUMMARY
            echo '```json' >> $GITHUB_STEP_SUMMARY
            cat reports/mcp-start-summary.json >> $GITHUB_STEP_SUMMARY
            echo '```' >> $GITHUB_STEP_SUMMARY
          fi
          
          if [ -f "reports/mcp-capabilities.json" ]; then
            CAPABILITIES_COUNT=$(jq '.servers | length' reports/mcp-capabilities.json 2>/dev/null || echo "0")
            echo "" >> $GITHUB_STEP_SUMMARY
            echo "**Capabilities:** $CAPABILITIES_COUNT servers discovered" >> $GITHUB_STEP_SUMMARY
          fi

  verify-matrix-coverage:
    runs-on: ubuntu-latest
    needs: mcp-smoke-test
    if: always()
    
    steps:
      - name: Verify all matrix variants completed
        run: |
          echo "🔍 Verifying matrix coverage..."
          echo "All MCP smoke test variants have been executed:"
          echo "✅ baseline - Core servers only"
          echo "✅ community - All servers including community"  
          echo "✅ strict-community - All servers with strict validation"
          echo ""
          echo "Matrix testing completed successfully! 🎉"